@model ElectronicsStore.AdminApp.Models.Account.RoleDeleteRequest

@{
    ViewData["Title"] = "Xóa vai trò";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Xóa vai trò</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Home">Trang chủ</a></li>
        <li class="breadcrumb-item"><a asp-action="GetRoles" asp-controller="Account">Danh sách vai trò</a></li>
        <li class="breadcrumb-item active">Xóa vai trò</li>
    </ol>
    <div class="card mb-4">
        <div class="card-header">
            <a class="btn btn-success" asp-action="GetRoles" asp-controller="Product">Về danh sách vai trò</a>
        </div>
        <div class="card-body">
            <div>
                <dl class="row">
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Id)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Id)
                    </dd>
                </dl>
                <form asp-action="DeleteRole">
                    <input type="submit" value="Xóa" class="btn btn-danger" />
                </form>
            </div>
        </div>
    </div>
</div>